#!/usr/bin/env fish
sudo apt-get -qq -y install git
git config --global user.name "Sol Kuczala"
git config --global user.email "sol.kuczala@gmail.com"
git config --global core.editor "nano"

mkdir -p ~/Desktop/workspace
cd
